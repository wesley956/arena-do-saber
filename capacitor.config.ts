import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wesley956.arenadosaber',
  appName: 'Arena do Saber',
  webDir: 'dist'
};

export default config;
